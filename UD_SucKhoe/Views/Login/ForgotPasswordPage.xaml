<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UD_SucKhoe.ForgotPasswordPage"
             Shell.NavBarIsVisible="False">

    <!-- Background gradient -->
    <ContentPage.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#E8F0FE" Offset="0.0"/>
            <GradientStop Color="#FFFFFF" Offset="0.5"/>
            <GradientStop Color="#F0F4FF" Offset="1.0"/>
        </LinearGradientBrush>
    </ContentPage.Background>

    <ScrollView>
        <Grid RowDefinitions="*,Auto,*" Padding="30" RowSpacing="0">

            <!-- Phần trên - Icon/Logo -->
            <VerticalStackLayout Grid.Row="0" 
                               VerticalOptions="End" 
                               Spacing="10"
                               Margin="0,0,0,30">

                <!-- Icon khóa -->
                <Border WidthRequest="80" 
                       HeightRequest="80"
                       HorizontalOptions="Center"
                       BackgroundColor="#7B68EE"
                       StrokeThickness="0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="40"/>
                    </Border.StrokeShape>
                    <Label Text="🔒"
                          FontSize="40"
                          HorizontalOptions="Center"
                          VerticalOptions="Center"/>
                </Border>
            </VerticalStackLayout>

            <!-- Form quên mật khẩu -->
            <Border Grid.Row="1"
                    StrokeThickness="0"
                    BackgroundColor="White"
                    Padding="30,35"
                    Shadow="{Shadow Brush=Gray, Offset='0,4', Radius=15, Opacity=0.15}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="20"/>
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="20">

                    <!-- Tiêu đề -->
                    <Label Text="Quên mật khẩu?"
                           FontSize="28"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           TextColor="#1A1A1A"/>

                    <!-- Mô tả -->
                    <Label Text="Nhập email của bạn để xác nhận và đặt lại mật khẩu"
                           FontSize="14"
                           TextColor="#666666"
                           HorizontalTextAlignment="Center"
                           LineBreakMode="WordWrap"
                           Margin="0,0,0,15"/>

                    <!-- Email input -->
                    <VerticalStackLayout Spacing="8">
                        <Label Text="Email"
                               FontSize="14"
                               FontAttributes="Bold"
                               TextColor="#333333"/>

                        <Border StrokeThickness="1.5"
                                Stroke="#E0E0E0"
                                BackgroundColor="#F8F9FA"
                                Padding="15,0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="10"/>
                            </Border.StrokeShape>

                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                                <!-- Icon email -->
                                <Label Grid.Column="0"
                                      Text="📧"
                                      FontSize="20"
                                      VerticalOptions="Center"/>

                                <!-- Entry -->
                                <Entry Grid.Column="1"
                                       x:Name="EmailPhoneEntry"
                                       Placeholder="example@email.com"
                                       Keyboard="Email"
                                       FontSize="15"
                                       TextColor="#1A1A1A"
                                       PlaceholderColor="#999999"
                                       VerticalOptions="Center"/>
                            </Grid>
                        </Border>
                    </VerticalStackLayout>

                    <!-- Nút xác nhận -->
                    <Button x:Name="SendCodeButton"
                            Text="Xác nhận"
                            FontSize="16"
                            FontAttributes="Bold"
                            TextColor="White"
                            HeightRequest="52"
                            Margin="0,15,0,0"
                            Clicked="OnSendCodeButtonClicked">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#7B68EE" Offset="0.0"/>
                                <GradientStop Color="#4A90E2" Offset="1.0"/>
                            </LinearGradientBrush>
                        </Button.Background>
                        <Button.Shadow>
                            <Shadow Brush="#7B68EE" 
                                   Offset="0,4" 
                                   Radius="10" 
                                   Opacity="0.3"/>
                        </Button.Shadow>
                    </Button>

                    <!-- Separator -->
                    <Grid ColumnDefinitions="*,Auto,*" 
                          ColumnSpacing="10" 
                          Margin="0,10,0,10">
                        <BoxView Grid.Column="0" 
                                Color="#E0E0E0" 
                                HeightRequest="1" 
                                VerticalOptions="Center"/>
                        <Label Grid.Column="1" 
                              Text="hoặc" 
                              FontSize="12" 
                              TextColor="#999999"/>
                        <BoxView Grid.Column="2" 
                                Color="#E0E0E0" 
                                HeightRequest="1" 
                                VerticalOptions="Center"/>
                    </Grid>

                    <!-- Nút quay lại -->
                    <Button Text="Quay lại đăng nhập"
                            FontSize="16"
                            FontAttributes="Bold"
                            CornerRadius="10"
                            TextColor="#7B68EE"
                            BackgroundColor="Transparent"
                            BorderColor="#7B68EE"
                            BorderWidth="2"
                            HeightRequest="52"
                            Clicked="OnBackButtonClicked">
                        
                    </Button>

                </VerticalStackLayout>
            </Border>

            <!-- Phần dưới - Footer -->
            <VerticalStackLayout Grid.Row="2" 
                               VerticalOptions="End" 
                               Margin="0,30,0,0">
                <Label Text="Cần trợ giúp? Liên hệ hỗ trợ"
                       FontSize="13"
                       TextColor="#7B68EE"
                       HorizontalOptions="Center"
                       TextDecorations="Underline">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnSupportTapped"/>
                    </Label.GestureRecognizers>
                </Label>
            </VerticalStackLayout>

        </Grid>
    </ScrollView>

</ContentPage>